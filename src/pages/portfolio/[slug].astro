---
import 'photoswipe/style.css';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Carousel from '$lib/components/Carousel.svelte';
import { fetchAllProjects, fetchProjectDataBySlug } from '$lib/data/portfolioData';
import { formatDate, isValidISODateString } from '$lib/utils/dateUtils';

export async function getStaticPaths() {
	const allProjects = await fetchAllProjects();
	return allProjects.map((project) => ({ params: { slug: project.slug } }));
}

const { slug } = Astro.params;
const project = fetchProjectDataBySlug(slug!);

if (!project) {
	throw new Error('Invalid slug');
}
---

<BaseLayout>
	<article class="project">
		<a class="btn-back" href="/portfolio"><i class="feathericon-arrow-left"></i>Back to Portfolio</a
		>

		<header class="header-project">
			<h1 class="title title--h1" set:html={project.pageTitle} />
			<div class="header-project__image-wrap">
				<img class="cover" src={project.cover.src} alt={project.cover.alt} />
			</div>
		</header>

		<section class="details-info details-info--inline">
			{
				project.infoBlocks.map((infoBlock) => (
					<li class="details-info__item">
						<span class="box icon-box">
							<i class={`font-icon ${infoBlock.icon}`} />
						</span>
						<div class="details-info__info">
							<span class="overhead">{infoBlock.title}</span>
							{isValidISODateString(infoBlock.content) ? (
								<span>{formatDate(infoBlock.content)}</span>
							) : (
								<span>{infoBlock.content}</span>
							)}
						</div>
					</li>
				))
			}
		</section>

		<section class="carousel">
			<Carousel
				client:visible
				swiperId="swiper-gallery"
				slidesPerView="auto"
				slides={project.images}
				centeredSlides={true}
				speed={300}
				breakpoints={{
					580: { slidesPerView: 'auto', spaceBetween: 20 }
				}}
			/>
		</section>

		<section class="project-copy" set:html={project.description} />
	</article>
</BaseLayout>

<script>
	import PhotoSwipeLightbox from 'photoswipe/lightbox';

	const lightbox = new PhotoSwipeLightbox({
		gallery: '#swiper-gallery',
		children: 'a',
		showHideAnimationType: 'fade',
		pswpModule: () => import('photoswipe')
	});
	lightbox.init();
</script>

<style lang="scss">
	@use 'styles/app/single-project';

	.details-info {
		margin-bottom: 1.5rem;
		@media only screen and (max-width: $small) {
			margin-bottom: 1rem;
		}
	}

	.carousel {
		margin-bottom: 2rem;
		@media only screen and (max-width: $small) {
			margin-bottom: 1rem;
		}
	}
</style>
